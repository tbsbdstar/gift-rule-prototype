# 原型发布工具 · 说明文档（交接用）

> 本文件是这套"HTML 原型 → GitHub Pages 在线链接"工具的完整说明。
> **换电脑、或新的 Claude 会话接手时，先读这份即可上手，不必重新解释。**

---

## 0. 一句话

把做好的 HTML 原型拖到 `发布HTML到在线.bat`，它就自动发布到 GitHub Pages、生成/更新 README（含在线链接与防缓存版本号）、并推送到 GitHub。产出可直接分享，或填进 Axure「内联框架」内嵌。

---

## 1. 关键信息

| 项 | 值 |
|----|----|
| 本地仓库 | 任意路径均可（脚本用 `$PSScriptRoot` 自动识别自身所在目录，原示例为 `C:\Users\taobs\gift-rule-site`） |
| 远端仓库 | `https://github.com/tbsbdstar/gift-rule-prototype.git` |
| 在线 base | `https://tbsbdstar.github.io/gift-rule-prototype/` |
| 目录页 | `https://tbsbdstar.github.io/gift-rule-prototype/catalog.html` |
| 首页 index.html | 固定 = 赠品规则源文件 `D:\claude workspace1\赠品规则\玛鲁丸_赠品规则系统_v1.5.html` |
| git 提交者 | 统一 `tbsbdstar` <52684455+tbsbdstar@users.noreply.github.com>（仓库 local 配置） |

## 2. 文件构成

- `发布HTML到在线.bat` → 调 `publish.ps1`：**发布/更新原型的主工具**
- `一键更新线上.bat` → 调 `deploy.ps1`：只刷新赠品规则首页 + 推送（不生成目录、不涨版本，一般用不到）
- `设置不列入.bat` → 调 `exclude-manager.ps1`：**交互式**设置某项目"不写入 README 链接"的页面（拖文件夹进去→列出页面→输编号切换）。被排除的页面仍在线，只是不列进 README。改完再跑 `发布HTML到在线` 生效。
- `publish.ps1`：核心脚本（顶部有分步注释）
- `_readme_template.md`：自动生成 README 用的模板（占位符 `{{PROJECT}}` / `{{PAGES_LIST}}` / `{{DIRECT_LINKS}}` / `{{AXURE_LINKS}}` / `{{PAGE_COUNT}}` / `{{DATE}}` / `{{PAGEHASH}}`）
- `catalog.html`：自动生成的目录页
- `index.html`：首页（赠品规则）
- `<项目名>/`：每个项目一个文件夹，内含该项目的 .html 与 `<项目名>_README.md`

## 3. 核心约定

### 3.1 一个项目 = 一个同名文件夹
每个项目在仓库里是一个**以项目名命名的顶层文件夹**，该项目的所有 .html、README 都放里面，绝不平铺在根目录。
在线 URL = `base + 项目文件夹/页面.html`（中文需 URL 编码）。
例外：`index.html`、`catalog.html`、`*.ps1`、`*.bat`、根 `README.md`、`_readme_template.md`、`工具说明.md` 留在根目录。

### 3.2 README 自动化（publish.ps1 负责）
遍历每个含 .html 的项目文件夹：
- **没有 `*_README.md`** → 用模板**自动生成**一份（v1，含所有页面的直接链接 + Axure `?v=1` 链接 + 版本记录 + 哈希标记）。功能说明是占位，建议由 Claude/人工补充。
- **已有 README** → 每次发布都做两件事：
  1. **自动刷新链接清单**：按文件夹里实际的 .html **重建**"直接链接 / Axure链接"两段（**新增页面自动出现、删除页面自动消失**），只替换 `<!--AUTO-LINKS:DIRECT-->…<!--/AUTO-LINKS:DIRECT-->` 和 `AXURE` 标记之间的内容，不动功能说明和版本记录。
  2. **内容变化才涨版本**：页面 MD5 变了才把所有 `?v=数字` +1、追加一条版本记录。
- **链接标记 `<!--AUTO-LINKS:DIRECT/AXURE-->`**：这对标记是脚本自动维护链接的锚点，**不要删**。若某 README 没有这对标记（老格式），脚本会跳过刷新该段——需先补上标记（见模板格式）。
- **排除某页面不列入 README**：项目文件夹里放 `_exclude.txt`（ASCII 名，脚本要引用故不能用中文名），每行一个要排除的**页面文件名**（`#` 开头为注释）。publish.ps1 会跳过这些页面（不列链接、不计入页数、不参与哈希），但页面仍会被推送、可通过直链访问。用 `设置不列入.bat` 交互维护此文件最省事。

### 3.3 防缓存版本号 `?v=N`
GitHub Pages 响应头 `Cache-Control: max-age=600`（10 分钟）。Axure/浏览器内嵌时，更新后可能看到旧内容。
解决：Axure 内联框架地址末尾带 `?v=N`；每次内容变化脚本自动 +1。**用户需把 Axure 里的数字同步成 README 里最新值。**
直接分享链接保持干净（不带 `?v=`）。

### 3.4 内容变化检测 = 页面哈希（不碰 git 输出）
对项目文件夹内所有 .html 按名称排序、各算 MD5、用 `-` 连接，存进 README 末尾 `<!-- pagehash:XXXX -->`。
下次发布重算，和存的比：变了才涨版本，没变不动（幂等）。
**为什么不用 `git diff` 判断**：Windows PowerShell 5.1 解析 git 输出的中文路径会码点截断、`Test-Path` 失败，不可靠。纯文件系统哈希绕开此坑。

## 4. Claude 的职责（会话在场时）
- 新项目首发后：读页面，把自动生成 README 里的**功能说明占位**补成真实描述（是什么 + 核心功能表格）。
- 页面有实质改动：更新功能说明文字。
- 版本号 `?v=` 和版本记录**交给脚本，不手动改**。
- README 里**不要写 `?v=2`/`?v=3` 这类字面示例**、也不要写"当前版本：vN"——只用 `?v=N` 作为唯一版本标记，否则会被脚本的正则误改。
- 页面间跳转（列表↔新增等）用**同文件夹内相对文件名**；改文件名后要同步改跳转，否则 404。

## 5. 常见问题
| 现象 | 原因 / 解决 |
|------|------------|
| 拖文件发布后没生成 md、看不到链接 | 脚本会自动生成 README（本次升级后）；老版本需 Claude 手动建。cmd 窗口一闪而过看不到链接属正常，链接在 README 里。 |
| Axure 内嵌点进子页 404 | 多为缓存：加载到旧版页面（旧跳转指向已改名/删除的文件）。把 Axure 地址换成最新 `?v=N` 并 Ctrl+F5 硬刷新。 |
| push 被拒 (rejected) | 远端有本地没有的提交（多因在 GitHub 网页上直接改过）。脚本已内置 `git pull --rebase` 自动合并；仍失败则联系 Claude。 |
| 新路径打开 404 但确实发布了 | GitHub Pages 构建/传播延迟，新建嵌套中文路径常需 1~3 分钟，稍等即可。 |
| 中文乱码 | 脚本里**不放中文字符串字面量**（PS5.1 读无 BOM 脚本按 ANSI 解码会乱码）；中文只放在数据文件（模板/README/本说明）和 `#` 单行注释里。 |

## 6. 换电脑 / 新会话怎么接手
1. 克隆仓库到**任意路径**（脚本用 `$PSScriptRoot` 自动识别自身所在目录当仓库根，**不必再改脚本里的路径**）。只要 `发布HTML到在线.bat` / `publish.ps1` / `deploy.ps1` 跟仓库在一起即可。
2. 设本地提交身份：`git config user.name tbsbdstar` + `git config user.email 52684455+tbsbdstar@users.noreply.github.com`。
3. 首页源文件路径 `$giftSrc`（赠品规则那份，在 D 盘、写死在脚本里）若不存在会自动跳过刷新首页，不影响其他功能。这是唯一还写死的外部路径——换电脑若还想让首页自动刷新，改 `publish.ps1`/`deploy.ps1` 顶部的 `$giftSrc` 即可。
4. 直接用 `发布HTML到在线.bat` 即可。新会话的 Claude 读本文件 + `publish.ps1` 顶部注释就懂全流程。
